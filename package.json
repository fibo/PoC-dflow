{
  "name": "dflow",
  "type": "module",
  "version": "1.0.0",
  "description": "is a minimal Dataflow programming engine",
  "scripts": {
    "build": "npm run generate_dflow.d.ts && npm run generate_dflow.js && deno fmt",
    "generate_dflow.d.ts": "npx tsc --build",
    "generate_dflow.js": "npx esbuild dflow.ts --format=esm --minify-whitespace --outfile=dflow.js --sourcemap",
    "test": "node --test"
  }
}
