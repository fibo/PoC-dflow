{
  "name": "dflow",
  "type": "module",
  "version": "1.0.0",
  "description": "is a minimal Dataflow programming engine",
  "scripts": {
    "build": "npm run generate_dflow.d.ts && npm run generate_dflow.js && npm run prettify",
    "generate_dflow.d.ts": "npx tsc --build tsconfig.declarations.json",
    "generate_dflow.js": "npx esbuild dflow.ts --format=esm --minify-whitespace --outfile=dflow.js --sourcemap",
    "prettify": "npx prettier --write .",
    "test": "node --test"
  }
}
